{
  "skill_name": "qupilot-quest-runner",
  "evals": [
    {
      "id": 1,
      "name": "list-and-recommend",
      "prompt": "Hey can you check my QuPilot quests and tell me which one to do first? I've got like 20 minutes before a meeting and I don't want to lose money on something dumb. My API key is in QUPILOT_API_KEY and the API is at https://api.qupilot.xyz/v1. I have byreal-cli and byreal-perps-cli installed.",
      "expected_output": "Agent calls GET /quests?status=open, parses the JSON envelope, presents a ranked list with reward, expires_at, and a one-line action summary per quest, and recommends one based on reward vs. estimated execution cost — being explicit about the uncertainty rather than promising a winner. Should NOT silently auto-execute.",
      "files": []
    },
    {
      "id": 2,
      "name": "execute-swap-quest-end-to-end",
      "prompt": "Please execute quest qst_01HXYABCDEF for me. I think it's the SOL→USDC swap one. Go ahead and do it, I trust you. QUPILOT_API_URL=https://api.qupilot.xyz/v1, QUPILOT_API_KEY is set.",
      "expected_output": "Agent reads qupilot-api.md, fetches the quest via GET /quests/:uuid to confirm action.kind = 'swap', POSTs /agent/participations with { quest_uuid } using x-api-key header to get participation.uuid (NOT claim_token), runs byreal-cli wallet balance check, runs `byreal-cli swap quote -o json` for a preview, validates quote against quest params (notional >= min_notional_usd, slippage <= max_slippage_bps), runs `byreal-cli swap execute --slippage-bps <param> -o json`, captures tx_hash from the JSON response, POSTs /agent/participations/:uuid/complete with flat { tx_hash } (no claim_token, no nested proof object), reads the synchronous response status (success or failed — verification is immediate, no polling needed), and reports the reward. If byreal returns success:false, submits complete with the failed tx_hash and reports the error verbatim. Should NOT invent CLI flags, should NOT poll after complete, should NOT use claim_token or nested proof, should use x-api-key header not Authorization: Bearer.",
      "files": []
    },
    {
      "id": 3,
      "name": "handle-unknown-action-kind",
      "prompt": "Run quest qst_01HXYWEIRDONE — it just came in and the description says 'maintain a Twitter post for 24 hours'. Same env vars as before.",
      "expected_output": "Agent fetches the quest, sees action.kind is something not in references/quest-mapping.md (e.g. 'social-post'), stops, does NOT claim, surfaces the unknown kind to the user with a clear message that the mapping table needs to be extended before this can be executed. Should NOT improvise by trying to run a byreal command that doesn't fit, and should NOT silently skip the quest.",
      "files": []
    }
  ]
}
